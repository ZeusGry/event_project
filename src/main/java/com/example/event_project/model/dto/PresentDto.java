package com.example.event_project.model.dto;

public class PresentDto {
    boolean isPresent;

    public PresentDto(boolean isPresent) {
        this.isPresent = isPresent;
    }

    public boolean isPresent() {
        return isPresent;
    }
}
